#include "main.h"
/**
 * puts2 - that prints every other character of a string
 *@str: a string
 */
void puts2(char *str)
{
	int i;
	char c;

	for (i = 0; i < 1000; i++)
	{
		if (i % 2 = 0)
		{
			c = str[i];
			_putchar(c);
		}
	}
}
